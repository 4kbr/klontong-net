// Titik masuk publik paket @klontong/api.
// Tipe bentuk API di-generate dari kontrak OpenAPI (`pnpm gen:api`) ke `schema.d.ts`.
// Fase 01 menambahkan client, endpoint `*.api.ts`, hooks Query, dan skema Zod di sini.
export type * from './schema';
